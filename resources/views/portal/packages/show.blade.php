@extends('portal.layout')
@section('title', $package->program_name)
@section('content')
    <a href="{{ route('portal.packages.index') }}" class="text-sm font-bold text-teal-700">← Kembali ke daftar paket</a>
    <section class="mt-5 overflow-hidden rounded-[2rem] bg-[#071827] p-6 text-white sm:p-8"><div class="grid gap-7 lg:grid-cols-[1fr_auto] lg:items-end"><div><p class="text-sm font-bold uppercase tracking-[.18em] text-teal-300">{{ $package->branch?->name }}</p><h1 class="mt-3 text-3xl font-extrabold sm:text-5xl">{{ $package->program_name }}</h1><p class="mt-4 max-w-3xl leading-7 text-slate-300">{{ $package->description }}</p></div><div class="rounded-2xl bg-white/10 p-5"><p class="text-xs text-slate-400">Harga paket</p><p class="mt-1 text-2xl font-extrabold">{{ $package->price ? 'Rp '.number_format($package->price, 0, ',', '.') : 'Hubungi cabang' }}</p></div></div></section>
    <div class="mt-6 grid gap-6 lg:grid-cols-[1fr_360px]"><div class="space-y-6"><section class="travel-panel p-6"><h2 class="text-xl font-extrabold">Informasi Perjalanan</h2><div class="mt-5 grid gap-3 sm:grid-cols-2"><span class="travel-chip"><i data-lucide="calendar-days" class="size-4"></i>{{ $package->departure_date->translatedFormat('d M Y') }} – {{ $package->return_date->translatedFormat('d M Y') }}</span><span class="travel-chip"><i data-lucide="plane" class="size-4"></i>{{ $package->airline ?: 'Menyusul' }} {{ $package->flight_number }}</span><span class="travel-chip"><i data-lucide="hotel" class="size-4"></i>{{ $package->hotels->pluck('name')->join(' & ') ?: 'Hotel menyusul' }}</span><span class="travel-chip"><i data-lucide="users" class="size-4"></i>{{ $package->remaining_quota === null ? 'Kuota fleksibel' : $package->remaining_quota.' kursi tersisa' }}</span></div></section><section class="travel-panel p-6"><h2 class="text-xl font-extrabold">Itinerary</h2><div class="mt-5 space-y-4">@forelse ($package->itineraries as $item)<article class="grid gap-3 rounded-2xl border border-slate-200 p-4 sm:grid-cols-[80px_1fr]"><strong class="text-sm text-teal-700">Hari {{ $item->day_number }}</strong><div><h3 class="font-extrabold">{{ $item->title }}</h3><p class="mt-1 text-sm text-slate-500">{{ $item->city }}</p><p class="mt-2 leading-6 text-slate-600">{{ $item->description }}</p></div></article>@empty<p class="text-slate-500">Itinerary sedang disiapkan.</p>@endforelse</div></section></div>
        <aside class="travel-panel h-fit p-6"><p class="text-sm font-bold uppercase tracking-[.15em] text-teal-700">Lanjutkan Pendaftaran</p><h2 class="mt-2 text-2xl font-extrabold">Paket ini sesuai?</h2><p class="mt-3 text-sm leading-6 text-slate-600">Setelah memilih, Anda akan diminta melengkapi biodata. Pembayaran dilakukan di kantor {{ $package->branch?->name }}.</p>@if ($package->remaining_quota === 0)<div class="mt-5 rounded-xl bg-amber-50 p-4 text-sm font-bold text-amber-800">Kuota paket sudah penuh.</div>@else<form method="POST" action="{{ route('portal.packages.select', $package) }}" class="mt-5">@csrf<button class="button-primary w-full">Pilih Paket Ini</button></form>@endif</aside></div>
@endsection
